import axios from './ajax'
import {processData, processData2, processTimeData, processObj, deepMerge, deepClone, getDeepObj, walkData, replaceSeparator} from './process-data'

export {
    axios,
    processData,
    processData2,
    processTimeData,
    processObj,
    deepMerge,
    deepClone,
    getDeepObj,
    walkData,
    replaceSeparator,
}
